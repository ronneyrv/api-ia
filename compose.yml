services:
  database:
    image: postgres:17

    environment:
      POSTGRES_DB: transaction
      POSTGRES_USER: app
      POSTGRES_PASSWORD: app

    ports:
      - "5432:5432"

    volumes:
      - transaction_data:/var/lib/postgresql/data